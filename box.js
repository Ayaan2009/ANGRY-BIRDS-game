class Box extends BaseClass {
    constructor(x, y, width, height) {
        super (x, y, width, height);
        this.image = loadImage("sprites/Box.png");
    }

}
   


 // blueprint using which we can design objects(with same prop and methods/member function of class) that are of same kind
 //special member function constructor() 
 //constructor stores properties
 //while definig a function, it needs parameters to work
 //body property of the object we are going to create using this blueprint/class
 //all properties will be assigned to objects that we create using box class
 //methods/member function of class)